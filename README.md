# Ledger — US Foreign Aid

Explorer for World Bank indicator `DC.DAC.USAL.CD` (net bilateral aid flows from the United States), 2001–2023.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
